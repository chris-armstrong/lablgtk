# Auto-generated GTK bindings — intentionally NOT linted or fixed (decision:
# generated code is excluded from all lint rules). merlint excludes them via
# merlint.toml. semgrep therefore only gates hand-written code.
**/generated/**

# Build artefacts (also in .gitignore; repeated for non-git contexts)
_build/

# opam build sandbox created by `opam install . --deps-only` (CI + local).
# Contains copies/symlinks of dependency source and must NOT be linted. Not
# in .gitignore, so without this semgrep scans it (1864 stray findings in CI).
_opam/
**/_opam/
